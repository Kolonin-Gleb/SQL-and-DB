# Тест прямого добавления информации в систему логирования

INSERT INTO hw2_logging(
    id,
    news_id,
    title_before_change,
    title_after_change,
    info_before_change,
    info_after_change,
    dt_edition,
    dt_before_change,
    dt_after_change
)
VALUES(
    0,
    1,
    'Заголовок до изменения',
    'Заголовок после изменения',
    'Текст новости до изменения',
    'Текст новости после изменения',
    'Измененная дата новости',
    'Время публикации до изменения',
    'Время публикации после изменения'
);

# Тест изменения существующей новости
UPDATE `hw2_news` SET `title` = 'Технологии', `info` = 'Знания SQL вышли в топ востребованных знаний!', `dt` = '2022-02-14', `author` = 'Глеб_из_IT' WHERE `hw2_news`.`id` = 1;

Эти тесты не должны срабатывать. В задании этого не требуется
# Тест удаления существующей новости
DELETE FROM `hw2_news` WHERE `hw2_news`.`id` = 1;
# Тест добавления новости
INSERT INTO `hw2_news` (`id`, `title`, `info`, `dt`, `author`) VALUES ('1', 'Футбол', 'Команда SQL победила!', '2022-02-13', 'Глеб');

