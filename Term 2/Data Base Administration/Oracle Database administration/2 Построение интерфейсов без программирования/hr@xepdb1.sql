-- 1.???????? SQL ??????, ??????? ?????? ?????? ??????????? ? ????????? ????????? ????

-- ???? ???? ? ??????????? (first_name, last_name, salary) +
-- ???? ????????? ???? (job_id, manager_id, department_id) +
-- ????????? ?? job_id, ????? ???????? jobs.job_title +
-- ????????? ?? manager_id, ???????? ? employees.employee_id (?? ???? ??? ?????? first_name, last_name) # ????????? ??????? +
-- ????????? ?? department_id, ????? ??????? ? departments.department_id (?? ???? ??? ?????? department_name) +


--???????
SELECT *
FROM employees




SELECT
    first_name, last_name, salary,
    job_title,
    department_name,
    (SELECT first_name FROM employees WHERE emp.manager_id = employee_id) as manager_first_name,
    (SELECT last_name FROM employees WHERE emp.manager_id = employee_id) as manager_last_name
FROM employees emp, departments, jobs
WHERE
    emp.job_id = jobs.job_id AND
    emp.department_id = departments.department_id;

-- ????? ????????? ?????? ??? 2?? (1 - Where, 2 - Select)

SELECT * FROM employees;
SELECT * FROM jobs;
SELECT * FROM departments;

